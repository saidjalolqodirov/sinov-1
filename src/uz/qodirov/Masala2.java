package uz.qodirov;

/**
 * created by: Qodirov Saidjalol
 * created at: 7/3/2022 10:25 AM
 */

public class Masala2 {
    public int numbersOfZero(int n) {
        return n >= 5 ? 1 : 0;

        //  N gaca bo'lgan tub sonlarni topish algaritmi

        /*boolean[] prime = new boolean[n + 1];
        for (int i = 0; i < n; i++) {
            prime[i] = true;
        }
        for (int p = 2; p * p <= n; p++) {
            if (prime[p]) {
                for (int i = p * 2; i <= n; i += p) {
                    prime[i] = false;
                }
            }
        */

    }
}

